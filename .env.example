# --- Database Configuration ---
# Name of the database to create/connect to
DB_NAME=auth

# The root password for the MySQL container
DB_ROOT_PASSWORD=your_secure_password_here

# --- Security Configuration ---
# Used to sign JWT tokens. Generate using: openssl rand -base64 32
JWT_SECRET=your_long_random_string_here
